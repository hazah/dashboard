document.getElementById("dashboard").
  dispatchEvent(
    new CustomEvent(
      "component:refresh", { 
        detail: {
          target: "network",
          data: "<%= j render "application/network" %>"
        }
      }
    )
  );

document.getElementById("dashboard").
  dispatchEvent(
    new CustomEvent(
      "component:refresh", { 
        detail: {
          target: "actions",
          data: "<%= j render "application/actions" %>"
        }
      }
    )
  );

<%= render "messages/refresh" %>
<%= render "messages/reset" %>
