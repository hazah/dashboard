document.querySelectorAll("button[data-location-id]").forEach(e => { e.classList.toggle('btn-secondary', false); });
document.querySelector("button[data-location-id='<%= @current_location.location_id %>']").classList.toggle('btn-secondary', true);