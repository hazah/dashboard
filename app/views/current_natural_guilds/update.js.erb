document.getElementById("dashboard").
  dispatchEvent(
    new CustomEvent(
      "component:refresh", {
        detail: {
          target: "concernArea",
          data: "<%= j current_concern_areas_select %>"
        }
      }
    )
  );
