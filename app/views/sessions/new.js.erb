document.getElementById("dashboard").
  dispatchEvent(
    new CustomEvent(
      "component:refresh", {
        detail: {
          target: "application",
          data: "<%= j render template: "sessions/new.html" %>"
        }
      }
    )
  );
