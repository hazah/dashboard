<%= button_to [:current, :context], class: "btn#{' btn-secondary' if current_context.context_id == context_button.context.id}", remote: true, method: :put, params: { id: context_button.context.id }, data: context_button_data(context_button.context) do %>
  <%= render context_button.context %>
<% end %>