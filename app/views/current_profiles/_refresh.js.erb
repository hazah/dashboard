document.getElementById("dashboard").
  dispatchEvent(
    new CustomEvent(
      "component:refresh", { 
        detail: {
          target: "networkLinks",
          data: "<%= j render "application/network_links" %>"
        }
      }
    )
  );

document.getElementById("dashboard").
  dispatchEvent(
    new CustomEvent(
      "component:refresh", {
        detail: {
          target: "newMessage",
          data: "<%= j render "application/new_message" %>"
        }
      }
    )
  );
