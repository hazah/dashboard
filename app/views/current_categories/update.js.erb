document.getElementById("dashboard").
  dispatchEvent(
    new CustomEvent(
      "component:refresh", {
        detail: {
          target: "categoryChild",
          data: "<%= j current_category_children_select %>"
        }
      }
    )
  );
